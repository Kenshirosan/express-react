import React from 'react';

const Profile = () => {
    return (
        <h2>
            User Profile : Faire un formulaire qui permet de mettre à jour les
            infos user
        </h2>
    );
};

export default Profile;
